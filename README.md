# vigilo-auto-login
Script to automatically log into Vigilo (vigilo.no).

I need to log in fast, the teenagers are going crazy. You need to edit the script and add the variables at the top. Requires python3-selenium and chromium-chromedriver on Ubuntu.
